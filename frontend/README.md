
  # Chatbot UI Integration

  This is a code bundle for Chatbot UI Integration. The original project is available at https://www.figma.com/design/GSqPhJ26hsdk2IGoncisAP/Chatbot-UI-Integration.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  